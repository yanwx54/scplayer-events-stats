# 2026-09-18

## 新建项目：Project07_scplayer-stats（三大赛事选手数据查询网站）

参考项目 `D:\WorkSpace\scplayer-stats`（旧版 60 人跟踪 + 全量对战查询），按用户要求新建一个
**数据来源严格限定在 eloboard 三大赛事** 的选手数据查询网站。

### 数据源（唯一）
- `https://eloboard.com/events/43` 메이저 프로리그 — 13,888 场
- `https://eloboard.com/events/33` K리그 — 12,925 场
- `https://eloboard.com/events/64` 준메이저 프로리그 — 1,348 场
- 合计 28,161 场 / 238 名选手 / 84 张地图，时间跨度 2021-04-08 ~ 2026-09-17

### 关键技术发现（重要，可复用）
1. **Bash 沙箱无直连外网**，但环境变量已配代理 `http_proxy=http://127.0.0.1:1256`。
   - `curl` 走该代理会卡在 TLS 重协商（schannel）→ HTTP 000，**不可用**。
   - **Node 原生 `fetch` 走同一代理完全正常**（OpenSSL 栈），所有抓取脚本用 Node fetch。
2. **本地预览时 curl 访问 127.0.0.1 会被代理拦截** → 必须加 `--noproxy '*'` 才能访问本机服务。
3. eloboard 公开 JSON API（无需过盾，与官网前端同源）：
   - `GET /api/matches?event_id={43|33|64}&limit=200&offset=n`
     - `limit` 上限 **200**（201 即报错）；总数在响应头 **`x-total-count`**
   - `GET /api/players/{id}` — `main_race` / `elo_raw` / `thumb_url` / `college_name`
   - `GET /api/events/{id}` — 赛事元信息
   - 头像基址 `https://eloboard.co.kr/static/`（`.co.kr`，不是 `.com`）
   - 注意：旧文档说的 `/api/events/{id}/matches` **不存在**（404）
4. 比赛记录为**单局粒度**，`participants` 恒为 2 人，`elo_delta` 恒为胜方正增益（235 场为 null）。
   赛事 43/33/64 均为 `is_team_entry: true`（队制赛事），`participants[].team_name` 可拿到队伍。

### 产出
- 抓取/构建脚本：`scripts/fetch-matches.mjs`、`fetch-players.mjs`、`fetch-avatars.mjs`、`build-db.mjs`、`verify.mjs`、`crosscheck.mjs`、`shoot.cjs`
- 网站（纯静态，无框架）：`public/index.html` + `app.js` + `style.css`
  - 视图：总览 / 选手排行 / 选手详情（5 标签）/ 双方对战 / 地图情报，hash 路由
  - 数据：`public/data/index.json`（选手索引，含分赛事胜负 `ev{43,33,64:{g,w,l}}`）、
    `players/{id}.json`（238 个，含月度/地图/对手明细）、`maps.json`
  - 头像本地化到 `public/avatars/`（234 张，3.7MB，规避外链被墙）
- 服务器：`server.mjs`（零依赖静态服务器，默认端口 5178）
- 校验：胜场总和 = 负场总和 = 28,161；변현제 vs 유영진 H2H 独立复算 246 场 139:107 完全一致

### 踩坑记录
- `public/app.js` 中两个 tab 渲染函数把 `draw()` 写在 `return` 之后 → 死代码，标签页空白。
  重构为 `tabXxx()` 只返回 HTML + `initXxx()` 负责渲染与事件绑定。
- H2H 路由提前 `return` 导致 `#h2hResult` 容器不存在 → `innerHTML of null`。
  改为始终渲染选择器 UI，再按 query 参数回填并渲染结果。
- Playwright 1.62.1 期望 chromium 1234，本机只有 1161/1200 →
  用 `chromium.launch({ executablePath: '.../chromium-1200/chrome-win64/chrome.exe' })`。
  playwright 复用参考项目 `D:\WorkSpace\scplayer-stats\node_modules`（本机无 agent-browser CLI）。

### 数据口径约定（用户明确要求）
- 所有战绩/地图/对抗统计**只从这三个赛事的比赛重新计算**，不含其他赛事。
- 页面上的「官方 ELO」是 eloboard 站点全局当前值，仅作参考；「ELO 净变」才是本口径内指标。

---

## 追加：每日 10:00 自动同步

用户要求「以后每天 10:00 同步这三个网址更新的战绩，并更新数据库」。

### 新增 `scripts/sync.mjs`（增量同步）
1. 从 `offset=0` 分页拉取（官方最新在前），逐页比对本地已知 `match id`。
2. `REFRESH_WINDOW = 400`：最近 400 条**每次强制重抓**，用于吸收官方对近期记录的修正/删除。
3. 越过窗口后遇到「整页都已存在」即停止 → 增量边界。
4. 合并规则：`Map` 以新抓到的覆盖同 id，旧记录保留（只增不减 + 窗口纠错）。
5. 自动补抓新选手元数据、下载缺失头像，最后 `spawn` 调用 `build-db.mjs` 重建。
6. 输出 `SYNC_OK` / `SYNC_FAILED`；若本地条数 < 官方 `x-total-count` 会提示用 `--full`。
- 参数：`--full` 全量重抓、`--no-build` 只抓不重建。
- 实测：无新数据时 2.5~5s（每赛事仅 2 页）；**已用「删掉最新 7 场再同步」验证能精确补回，恢复后与备份 JSON 完全一致**。

### 自动化
- 名称「SC三大赛事数据每日同步」，id `6d509d81-1957-43f4-be6a-80da6aa7fa96`
- RRULE `FREQ=DAILY;BYHOUR=10;BYMINUTE=0`，cwd = 项目根，下次运行 2026-09-19 10:00
- 流程：`sync.mjs` → 失败则 `--full` → `verify.mjs` → 输出中文简报（≤12 行）

### 其他
- `scripts/verify.mjs` 重写：索引结构从 `e43/e33/e64` 改为 `ev{43,33,64:{g,w,l}}` 后原校验读不到字段，
  现改为校验分赛事场次合计 == 对局数 ×2、胜=负、无重复 id、抽样选手与原始数据独立复算一致。
- 站点静态托管，同步后无需额外部署步骤。

---

## 追加：本地 Git 仓库 + GitHub 推送

用户要求「先做本地 Git 仓库，备份。然后推送到 GitHub」。

### 本地仓库（已完成）
- `git init -b main`，仓库级身份：`yanwx54 <yanwx54@users.noreply.github.com>`
  - 注意：全局 `user.email` 是占位符 `你的GitHub注册邮箱`（无效），参考项目的历史提交也带着这个占位符。
    这里改用 GitHub noreply 地址，既能归属账号又不暴露真实邮箱。
- 首次提交 `59e90b3`，491 个文件，`.git` 6.3MB，工作区干净。
- **`.gitignore` 决策**：排除 `data/raw/`、`shots/`、`node_modules/`
  - 理由：`data/raw/event-43.json` 单文件 10MB、`event-33.json` 9MB，**每日同步会整体重写**，
    若入库每天新增 ~1MB 压缩 blob，一年膨胀数百 MB。它是可再生的抓取缓存（缺了 sync.mjs 会自动全量重抓）。
  - 保留 `public/data/`（15MB）+ `public/avatars/`（3.7MB）：这是网站的实际数据产物；
    每日只有当天出场过的选手文件内容变化，其余文件内容哈希不变 → git 不会产生新 blob，膨胀可控。

### GitHub 连通性（关键发现）
- **SSH 认证已通**：`ssh -p 443 -T git@ssh.github.com` → `Hi yanwx54! You've successfully authenticated`
  - `~/.ssh/config` 已把 `github.com` 映射到 `ssh.github.com:443`
  - 注意：`ssh -T git@github.com` 与 22 端口**间歇性** "Connection reset by peer"，
    443 端口稳定。重试通常能过。
- HTTPS 走代理：`github.com`、`api.github.com`、`codeload.github.com` 均可访问（早先一次 fetch failed 是瞬时抖动）。
- `raw.githubusercontent.com` 不通；`objects.githubusercontent.com` 返回 404（正常）。
- **gh CLI 未登录**（无 `~/.config/gh`），环境变量里也没有 token → 无法用命令行建仓库。

### 待办
- 用户选择：仓库名 `scplayer-events-stats`、**公开**、自己在网页建空仓库。
- 已配置 `origin = git@github.com:yanwx54/scplayer-events-stats.git`。
- 轮询 90 秒仍未检测到仓库创建 → 等用户建好后执行 `git push -u origin main`。
- ⚠️ 提醒过用户：仓库含 234 张从 eloboard 下载的选手头像，公开仓库有版权外链风险，用户仍选公开。

### 推送完成（2026-09-18 11:02）
- 用户建好空仓库后，`git push -u origin main` 成功。
- 已通过 GitHub API 独立核验远程内容：491 个文件、2 个提交、目录结构与本地一致，
  README/package.json/server.mjs/public/*/scripts/sync.mjs 等关键文件全部可访问。
- **三方一致**：本地 HEAD = `origin/main` = 远程 `refs/heads/main` = `ef03f34`，工作区干净。

### ⚠️ 沙箱踩坑：`.git/refs/remotes/origin/` 写不进去
- `git fetch` 输出 `* [new branch] main -> origin/main` 看似成功，
  但 `.git/refs/remotes/origin/` 目录**始终不存在**，导致 `git status` 显示 `## main...origin/main [gone]`。
- 根因：沙箱的 brokered filesystem shim 拦截了该路径的写入（`mkdir -p` 也返回成功但不落盘）。
- **解决办法：改用 `.git/packed-refs`**，直接写入
  `ef03f34... refs/remotes/origin/main`，之后 `git rev-parse origin/main`、`git branch -r` 均正常。
- 经验：这个环境里凡是 `.git/refs/` 下的深层嵌套目录写入都不可靠，优先走 packed-refs。
- 注意：这是沙箱环境的问题，用户在**自己的终端**里不会遇到，`git fetch` 一切正常。

### 遗留建议（待用户决定）
- 每日自动化目前只更新本地文件，**不会自动提交/推送**。若要真正的"备份"语义，
  需要在自动化里加 commit + push 步骤。顾虑：`public/data/players/*.json` 每天有 10~20 个文件变化，
  未 gc 时每天约 1.5MB 松散对象；但 `git gc` 后 packfile 会做跨版本 delta 压缩，长期增长可控。
  → **用户已选择「每天自动提交推送」**，见下。

### 追加：每日自动提交推送（用户选定）
- 新增 `scripts/git-backup.mjs`（npm run backup）：
  - 无变更时输出 `GIT_BACKUP_SKIP`，不产生空提交
  - 有变更则 `git add -A` → commit（消息含日期与 shortstat）→ push，**失败退避重试 4 次**
    （SSH 到 github.com 会间歇性 Connection reset）
  - 推送后自动用 `.git/packed-refs` 校准 `origin/main` 追踪引用（绕过沙箱限制）
  - 末尾 `git gc --auto`；输出 `GIT_BACKUP_OK`
  - 用 `Atomics.wait` 实现同步 sleep（Node 无内置 sleepSync）
- 自动化 `6d509d81-1957-43f4-be6a-80da6aa7fa96` 已更新为三步：sync → verify → git-backup → 中文简报。

### ⚠️ 重要修复：避免每日空提交
- **问题**：`build-db.mjs` 每次都会写入 `meta.builtAt` 时间戳，导致即使当天没有任何新对局，
  `public/data/index.json` 也会产生 diff → 每日自动化会天天生成一个无意义的提交。
- **修复**：`sync.mjs` 增加内容级变更检测
  - `syncEvent()` 读入原始文本，与合并后的 `JSON.stringify(merged)` 比对，返回 `changed`
    （只比 id 增减不够——官方「修正」某条记录时条数不变但内容变了）
  - `main()` 里 `dataChanged = 任一赛事 changed || 新增选手 || 新增头像`
  - `dataChanged === false` 时**跳过 build-db**，`public/data/` 完全不动 → git-backup 自然 SKIP
- 效果：无比赛的日子整条链路 2.6s 跑完且零提交；有比赛的日子才重建+提交。

### 最终状态
- 远程 `yanwx54/scplayer-events-stats`（公开），main 分支，工作区干净
- 端到端实测：sync `SYNC_OK` → verify 11 项全 ✓ / 0 项 ✗ → git-backup `GIT_BACKUP_OK`

---

## 第二轮：选手排行 / 双方对战 的日期时间段筛选

提交 `6a49724`（已推送，本地 = 远程 = `6a49724`）。

### 数据层：新增 `public/data/daily.json`
区间筛选若每次遍历 238 个选手明细（13.8MB）会很慢，因此在 `build-db.mjs` 里额外产出按
「**日期 × 赛事**」分桶的稀疏矩阵：
- `days: ["2021-04-08", ...]` 日期轴 + `p[选手id] = [[赛事id, 日期下标, 场次, 胜场, ELO净变], ...]`
- 粒度选「日期 × 赛事」而非「日期」，是为了让「赛事 + 日期区间」能任意组合，无需为每种组合预计算
- 前端用 `lowerBound`/`upperBound` 对日期轴二分定位后线性求和，O(桶数)
- 区间为「全部」且未选赛事时直接复用 `index.json` 已有总量，不走桶求和
- 体积 476KB / 1718 天 / 25,199 个桶

### 前端 `public/app.js`
- 新增 `RANGE_PRESETS` / `rangeBarHTML()` / `bindRangeBar()` / `sumBuckets()` / `isFullRange()`
- 排行：`view(p)` 按当前「赛事 + 区间」投影出用于展示与排序的数值；ELO 净变列提前并纳入排序
- H2H：新增 `#h2hRange`，`draw()` 重绘；换选手时按 `pairKey` 比对重置区间，避免残留
- `state.h2h` 增加 `pair` 字段；`state.playerFilter` 增加 `from` / `to`

### 关键 bug：event 筛选跨页面残留
- **现象**：`shoot.cjs` 断言「恢复『全部』后与初始一致」失败，
  初始 `변현제 2,294`（其实是赛事 43 的数量）→ 恢复后 `2,897`。
  一度误判为「测试脚本测量时机问题」，实际是**产品缺陷**。
- **根因**：`viewPlayers` 里 `if (params.get('event')) state.playerFilter.event = ...`
  —— 只在 URL 带参数时赋值，**从不复位**。首页事件卡片正是用 `#/players?event=43` 跳转的，
  于是访问过之后，再点导航「选手排行」（`#/players`）仍停留在赛事 43，而地址栏看不出来。
- **修复**：以 URL 为唯一事实来源，无参数一律归零；切换赛事时用 `history.replaceState`
  同步地址栏（不触发 hashchange，不会整页重渲染）。
- 修复后原先「对不上」的数字全部自洽：2025 全年榜首由 `유영진 280`（实为赛事 43 单赛事）
  变为全赛事 `방태수 447`；近 90 天榜首由 151 → 168 场。
- **教训**：断言失败时先怀疑产品，不要先怀疑测试。

### 校验与自检
- 新增 `scripts/verify-range.mjs`：抽 56 组「赛事 × 日期区间」与原始记录复算比对 → `RANGE_OK`
- `shoot.cjs`：22 项断言全通过、控制台零错误 → `SHOOT_OK`；
  新增回归断言「离开 `?event=43` 后复位为『全部赛事』」，
  基线期望改为从 `index.json` 推导（避免数据每日增长后写死的数字失效）
- `npm run verify:range` 加入 package.json；每日自动化新增该步骤（现为 sync → verify → verify-range → git-backup）

### 其他
- `git-backup.mjs` 提交信息按改动内容生成：只有 `public/data/`、`public/avatars/` 变化才用
  `chore(data)`，涉及脚本/文档用 `chore`（此前把改脚本的提交也写成「每日同步」，误导）
- 清理临时调试脚本 `dbg.cjs`、`probe-days.mjs`
- 严格遵守：未改动 `D:\WorkSpace\scplayer-stats` 等其他项目

### 当前状态
- 远程 `yanwx54/scplayer-events-stats`，main = `6a49724`，工作区干净
- 数据：28,161 场 / 238 名选手 / 84 张地图（2021-04-08 ~ 2026-09-17）

---

## 第三轮：中文译名 + 仅本赛季地图 + 数据收敛到 2026 年

提交 `ffecd2c`（已推送，本地 = 远程）。

### 需求（用户提供两份文档）
1. `docs/韩国选手名字.md`：文档内选手按中文名显示；排行/双方对战搜索支持 韩文 / 中文 / 英文ID
2. `docs/地图翻译规则.md`：文档内地图用中文，其余保留韩文；**所有地图板块只显示本赛季地图**
3. 只要 2026-01-01 之后的数据

### 关键判断：先探查再动手
动手前先跑了一次数据探查，两个发现直接决定了实现方式：
- **2026+ 共 4,004 场**（43:2175 / 33:1376 / 64:453），86 名选手
- **本赛季 7 张地图只覆盖 2026+ 的 55.6%（2,225 场）**，另外 1,779 场（44.4%）在
  2026 上半年的旧地图池（폴스타 / 네오 실피드 / 매치포인트 / 제인 도 …）。
  按月看，地图池是 **2026-07 切换**的：7 月前池外占多数，7 月后池外几乎为 0。
  → 因此「只显示本赛季地图」会让这 1,779 场不出现在任何地图板块。
  按需求字面实现（保留在选手总场次里，只是不进地图板块），并在对局记录里给非本赛季
  地图加「·非本赛季」标注，README 里写明，最后在回复里向用户点出这个取舍。
- 选手译名覆盖：**52 / 86 命中**，覆盖 98.4% 出场场次；文档外 34 名都是零星出场（最多 21 场）

### 实现要点
- `build-db.mjs`：
  - `CUTOFF = '2026-01-01'`（可用环境变量覆盖），过滤比赛；raw 保留全量，改常量即可调整
  - `PLAYER_ROWS` / `MAP_ROWS` 两张表内建在脚本里（不解析 Markdown，避免运行时依赖），
    按 `normName()`（去空格 + 小写）容错匹配，输出 `cn` / `idEn`
  - `SEASON_ROW_IDX` 指向 MAP_ROWS 下标，定义本赛季 7 张；`isSeasonMap()` 用于过滤
  - 选手 `maps` 数组与 `globalMap` 都只保留本赛季地图 → 前端所有地图区块自动收敛
  - `meta` 新增 `cutoff` 与 `season: { label, aliases, maps }`
- `app.js`：新增 `dispName()` / `nameHTML()` / `matchPlayer()` / `mapHTML()` / `mapTag()` /
  `mapCn()` / `P_BY_ID` / `SEASON_MAPS`；`oppName()` 改为走 `P_BY_ID` 并优先中文名
- 顺带修掉一个真问题：**区间内 0 场出场的选手会照样列进排行**（选在数据起点之前会出现 86 行「0 场」），
  改为 0 场不进排行 + 空状态提示

### 校验脚本同步口径（都要跟着改，否则会误报）
- `verify.mjs`：抽样选手由写死的 #34 改为**动态取 index 榜首**；新增译名覆盖、地图口径断言
- `verify-range.mjs`：`brute()` 复算侧也要按 CUTOFF 钳制（否则 2021 起点的用例会全错）
- `crosscheck.mjs`：同样加 CUTOFF
- `shoot.cjs`：所有期望值改为**从构建产物推导**，不再写死数字（数据每天都在涨）
  - 新增：中文名+韩文并列显示、三语搜索（迷你 / 변현제 / Mini / Flash）、
    仅本赛季地图（地图情报 7 行、H2H 分地图、选手地图 tab）、早于数据起点无数据
  - 踩坑：H2H 搜索后没 blur，下拉框盖住了区间条按钮 → Playwright 点击超时。
    加 `blur()` + 断言下拉收起

### 结果
- `verify.mjs` 全 ✓ / `verify-range.mjs` 64 项全通过 `RANGE_OK` / `shoot.cjs` 33 项全通过 `SHOOT_OK` 零控制台错误
- 体积：players 13.78MB → **2.14MB**，index 71.4KB → 29.2KB

### 当前状态
- 远程 `yanwx54/scplayer-events-stats`，main = `ffecd2c`，工作区干净
- 数据：4,004 场 / 86 名选手 / 本赛季 7 张地图（2026-01-01 ~ 2026-09-17）
- 改译名/地图范围的流程已写进 README：改 docs → 同步 build-db 里的常量 → `npm run build && npm run verify`




