# 2026-09-20

## 拉取 GitHub 最新版本（用户要求）

本地 `D:\WorkSpace\Project07_scplayer-stats` 原本停在 `00b810a`（9-18 我这一轮的收尾），
远程 main 已到 `dc4dcd7`（另一台机器/另一轮工作推的），**落后 5 个提交**：

| commit | 内容 |
|---|---|
| `24207a6` | fix(sync): 选手抓取改用列表接口（详情接口稳定 500）+ build-db 不再整目录删除 |
| `b8121f1` | chore: 记录 2026-09-19（全量重抓 + 接口修复 + 新 sandbox 发布） |
| `4a94ae7` | feat(deploy): 服务器自助定时更新（cron + self-sync.sh），兼容 Node 16 |
| `8b703a5` | fix(deploy): self-sync 无 rsync 时做真正的镜像同步 |
| `dc4dcd7` | chore: 记录服务器自助定时更新 |

- 拉取方式：`git fetch origin main` → `git merge --ff-only FETCH_HEAD`（**快进，无冲突**）
  - 本地未提交的 `2026-09-18.md` 与远程改动的 `MEMORY.md` / 新增 `2026-09-19.md` **不重叠**，所以安全
- 拉取后本地 = 远程 = `dc4dcd7`

### 顺带发现：本地 `data/raw/` 是旧的，必须补同步
`data/raw/` 是 **gitignore**（21MB 抓取缓存），不随拉取更新 → 拉取后
`public/data/`（新版，赛事 43 = 2189 场）与本地 raw（旧版，2175 场）不一致，`verify` 会失败。
跑一次 `node scripts/sync.mjs` 补齐 14 场（28,175 条，耗时 13.9s）后自洽。

## ⚠️ 发现并修复一个数据回归：选手战队名（college）丢失

### 现象
对比 `00b810a`（拉取前）与 `dc4dcd7`（拉取后）：**56 个选手的 `college` 从真实战队名变成了 `null`**
（如 김윤환 캄몬스타즈、이윤열 BGM、도재욱 뉴캣슬…）。

### 根因（两层）
1. **`syncPlayers` 只抓「本地还没有的选手」** —— 已有选手的元数据**永不刷新**，
   于是早期用详情接口抓的条目一直留着，生涯战绩也越来越旧。
   另一台机器重跑时，`players.json` 走的是**列表接口**，而列表接口的 `college_name` 恒为 null。
2. **两个接口的字段不一样**（实测）：
   - 列表 `/api/players?limit=&offset=` → 有 `college_id`，但 **`college_name` 恒为 null**（0/200 非空）
   - 详情 `/api/players/{id}` → **才有真实 `college_name`**，但它对不少 id **稳定 500**（如 player 100 신상문）
   - 而 `build-db.mjs` 是 `college: raw.college_name || null` → 列表抓来的条目全部写 null

### 我一开始搞错了方向（记录以免重犯）
先以为是「列表接口更全」，于是把 235 人全部用列表接口刷新 →
结果 `college` **全变 null，比拉取后还差**。是精确核对两个接口的 `college_name` 才定位到真因。

### 正确解法（已实现）
发现 **`/api/colleges`** 一次返回全部 13 支战队（`id` / `name` / `image_path` / `is_disbanded` / …）
→ 用它把 `college_id` 解析成名称，**稳定且只要 1 个请求**，不依赖会 500 的详情接口。

`scripts/sync.mjs` 改动：
- 新增 `fetchColleges()`：拉 `/api/colleges` 建 `id → name` 映射
- `syncPlayers()` 重写：
  - **刷新所有在册选手**（不再只补新增）→ 生涯战绩（wins/losses/last_played_on/elo）每天保持最新
  - 列表接口拿到的记录，`college_name` 为空时用 colleges 映射补齐；
    补不到再退回「原值且 `college_id` 相同」的旧值，**绝不把已知战队名冲成 null**
  - 返回值从 `added` 改为 `refreshed`（`dataChanged` 判断同步更新）
- 成本：选手列表 7 页 + 战队 1 个请求 ≈ 8 个请求，实测同步仍只要 8.8s

### 修复结果（三方对比）
| 对比基准 | 结果 |
|---|---|
| vs `00b810a`（拉取前） | 战队名一致 **85 / 86**，仅 1 人不同 |
| vs `dc4dcd7`（拉取后，含回归） | **修复了 55 个**被冲掉的战队名 |
| 生涯胜场比拉取前更新 | **17 / 86** 名选手（说明刷新确实带回了新数据） |

- 唯一例外：**player 100 신상문** 战队名 `더블비` → `null`。
  官方列表接口 `college_id=null`、详情接口 500 → **按接口为准，他当前确实没有战队**，属正常变化。
- 校验：`verify.mjs` 全 ✓（`VERIFY_DONE`）、`verify-range.mjs` 64 项 `RANGE_OK`

## 顺手修掉一个线上控制台错误（H2H 页面）

线上验收时 `shoot.cjs` 报出 **1 条控制台错误**（9-18 是 0 条）：

```
TypeError: Cannot set properties of null (setting 'innerHTML')
    at draw (app.js:1040) ← renderH2H (1109) ← viewH2H (994)
```

- **不是拉取引入的**：`git diff 00b810a dc4dcd7 -- public/app.js` 为空，前端一行没动
  → 是我 9-18 那版就埋下的隐患，今天才被触发
- 根因：`renderH2H` 是 **async**（要 `await loadPlayer`），而 `draw()` 里用的是
  **全局 `$('#h2hBody')`**。若渲染期间用户切走页面，`#h2hResult` 已被替换/移除，
  全局查询取到 `null` → 抛错
- 修法：改成**按 `host` 局部查找并持有引用** + 绘制前判断 `isConnected`
  ```js
  const body = host.querySelector('#h2hBody');
  const draw = () => {
    if (!body || !body.isConnected) return; // 页面已切走，放弃这次绘制
    body.innerHTML = `…`;
  };
  ```
- 教训：**异步渲染函数里不要用全局 id 查询**，要在渲染开始时把元素引用捕获下来；
  同类写法（`$('#xxx')` 在 async 之后使用）都值得排查
- 修复后线上 `SHOOT_OK  失败断言 0 项，控制台错误 0 条`

## 本轮最终状态
- 提交：`a676352`（sync 修复 + 战队名）→ 线上已部署
- 线上 `http://199.180.116.188:5001/`：4,018 场 / 86 选手 / 截至 2026-09-18，
  **战队名 55/86 已恢复**，71 项断言全过、0 控制台错误
- `check-source.mjs 43 --live`：官方 13902 = 本地 13902，CUTOFF 后 2189 = index.json 2189 ✓

## 环境备忘
- 本地 Node 22 下 `node -e` **不能同时用 `require()` 和顶层 `await`**（`ERR_AMBIGUOUS_MODULE_SYNTAX`）
  → 用 `(await import('node:fs')).default`
- `git ls-remote origin` 第一次报 `Permission denied`、紧接着重试就正常 —— **SSH 偶发抖动，重试即可**

## 10:09 每日自动化跑批（无新战绩）

`sync.mjs` → `verify.mjs` → `verify-range.mjs` → `git-backup.mjs` → `push-server.mjs`，全绿：

- 同步：43=13902 / 33=12925 / 64=1348，**三个赛事均无新增、无纠正**；
  官方选手 1268 人 / 13 支战队；在册 238 人，头像无新增 → **数据无变化，跳过重建**
- 校验：`VERIFY_DONE`（4,018 场 / 86 选手 / 截至 2026-09-18，平衡、抽样、地图 6 方向全 ✓）
- 区间：`RANGE_OK`（64 项通过）
- GitHub：`GIT_BACKUP_OK`，提交 **`7474be4`**（本次唯一变更是把 MEMORY.md 从 174 行压缩到 83 行）
- 线上：`✓ 线上已是最新，无需推送`（326 文件 / 5.5MB 与服务器 sha1 完全一致）
- 顺带把 `MEMORY.md` 去重整理（原文过长被系统截断），信息未丢
